core_version_requirement: '^8.7.7 || ^9.0.0'
name: 'Managed file purge'
type: module
description: 'Provides a generic external cache invalidation API and queue service for managed file URLs.'
package: "Purge - reverse proxies & CDNs"
configure: managed_file_purge.admin
dependencies:
  - drupal:file
  - purge:purge
